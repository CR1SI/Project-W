class_name Fireball
extends base_script

#if we need a ready function here make sure to call .ready() first so it doesn't override the main script
func deal_damage(_dmg: int):  #overrides the dmg func in the base script
	pass
